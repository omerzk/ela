input {
  file {
    path => "size_log"
    #start_position => "beginning"
  }
}
filter {
	# size of the second log
  grok {
    match => { "message" => "%{NUMBER:log_size}" }
  }
}
output {
  elasticsearch {
    hosts => ["localhost:9200"]
  }
}

